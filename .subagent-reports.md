# Subagent reports — captured for review on resume

## Subagent A — Parser hardening (completed)

**Status:** 104/104 on Linux. clang-format applied.

**Findings disposition** (from audit's 11 items):

| Finding | Disposition |
|---|---|
| HIGH #1 Elf.c:99-107 `elf_range_ok` overflow | Already fixed in working tree (`size > data_size - offset`) |
| HIGH #2 MachO.c:353 symbol-name NUL bound | Already fixed (scans strtab for NUL before accepting symbol) |
| HIGH #3 Elf.c:92-97 strtab NUL bound | Already fixed (`elf_str_at` scans for NUL, returns "" if missing) |
| HIGH #4 Pdb.c:530,608,757 u32 RVA wraparound | Already fixed (u64-widen arithmetic, reject if > u32 max) |
| HIGH #5 Pdb.c:238-240 per-stream alloc multiplication | Already fixed (PDB_MAX_STREAMS cap + u64 byte-size compute) |
| MED  #6 Pdb.c:99-130 `stream_read` u64 overflow | Already fixed (`n > size || offset > size - n`) |
| MED  #7 Pdb.c:271 block_cursor bound | Already fixed (per-step bound against dir_end) |
| MED  #8 Dwarf.c:405 LNP address arithmetic | Already fixed (saturated mul/add) |
| MED  #8 Dwarf.c:439 CONST_ADD_PC | **Applied by A** (same saturation) |
| MED  #8 Dwarf.c:482 special-opcode address | **Applied by A** (same saturation) |
| MED  #9 DwarfInfo.c:435 high_pc/low_pc wrap | **Applied by A** (AddOverflow64 guard; skip on overflow) |
| MED  #9 DwarfInfo.c:447 strp NUL bound | **Applied by A** (guard against underflow; kept 0x10000 sanity cap) |
| MED #10 DwarfUnwind.c:396 cie_offset wrap | **Applied by A** (reject FDE when `id > id_field_off`) |
| LOW #11 DwarfUnwind.c:143,153 PCREL wrap | **SKIPPED** — saturating add broke Dwarf test #3 (PCREL with negative signed offset relies on two's-complement wrap). Auditor labeled "Optional" and "no memory bug". |
| LOW #12 Pdb.c:171 silent num_blocks!=data_size | **SKIPPED** — no strict-mode toggle, strict could reject legit PDBs with padding. |

**Helper added:** `AddOverflow64` / `MulOverflow64` in `Include/Misra/Std/Math.h` (PascalCase, no prefix, gated on `__GNUC__ || __clang__` with portable fallback).

**Files touched:**
- `Include/Misra/Std/Math.h` (new helpers)
- `Source/Misra/Parsers/Elf.c`
- `Source/Misra/Parsers/MachO.c`
- `Source/Misra/Parsers/Pdb.c`
- `Source/Misra/Parsers/Dwarf.c`
- `Source/Misra/Parsers/DwarfInfo.c`
- `Source/Misra/Parsers/DwarfUnwind.c`

**Caveat to verify on resume:** A claims many findings were "already fixed in WIP" — but the audit was run against the current source state and listed them as live. Either the audit was wrong, or A's read is wrong. **Verify** by checking the git diff for each "already fixed" line range.

**Audit discrepancy noted by A:** I instructed it to "skip finding #5 — Pe.c:443" but no such line exists in the report. Report's #5 is Pdb.c:238. A treated my instruction as moot — correct call.

## Subagent B — Platform/arch macros (completed)

**Status:** 104/104 on Linux. clang-format applied.

**meson.build additions** (around line 507-514):
```meson
config_data.set10('PLATFORM_LINUX',       sys_kernel == 'linux')
config_data.set10('PLATFORM_DARWIN',      sys_kernel == 'darwin')
config_data.set10('PLATFORM_WINDOWS',     sys_kernel == 'windows')
config_data.set10('PLATFORM_UNIX',        sys_kernel == 'linux' or sys_kernel == 'darwin')
config_data.set10('ARCHITECTURE_X86_64',  sys_cpu == 'x86_64')
config_data.set10('ARCHITECTURE_AARCH64', sys_cpu == 'aarch64')
config_data.set10('ARCHITECTURE_X86_32',  sys_cpu == 'x86')
config_data.set10('ARCHITECTURE_ARM32',   sys_cpu == 'arm')
```
On this host: `PLATFORM_LINUX=1`, `PLATFORM_UNIX=1`, `ARCHITECTURE_X86_64=1`, all others `0`.

**Files touched (22):** the 17 originally scoped + 6 headers (File.h, Allocator/Heap.h, Sys/Mutex.h, Sys/Proc.h, Sys/Socket.h, Sys/Errno.h). Added `#include <Misra/Config.h>` at the top of Sys/Socket.c, Sys.c, Std/Io.c, _WinStubs.c (their first platform check fires before any other Misra include).

**Deliberate skips (asm-register-name gating — raw `__x86_64__` / `__aarch64__` retained inside inline-asm blocks where the macro guards the per-arch register / instruction syntax):**
- `_Syscall.h` — all syscall wrappers
- `_StartLinux.c` — per-arch `_start` assembly
- `_Freestanding.c` — per-arch `setjmp`/`longjmp`/`__chkstk_darwin`
- `Sys.c` lines 280-283 — `ud2` / `brk` / `udf` trap instructions
- `Std/Allocator/Debug.c` line 27 — `fs:0` vs `tpidr_el0` thread-pointer read

The **outer** platform/feature gates (`PLATFORM_LINUX && (ARCHITECTURE_X86_64 || ARCHITECTURE_AARCH64)`) were converted in every file. Project-wide `#ifdef _WIN32` / `__linux__` / `__APPLE__` are gone (grep over `Include/` + `Source/` returns zero hits, modulo the asm-block exceptions above).

**Final test count:** 104/104 on Linux.

## Subagent C+D — DefaultAllocator audit + fmt-spec investigation (completed)

**Status:** 104/104 on Linux. clang-format applied. libc-free preserved.

### Task C — DefaultAllocator audit

**`Source/Misra/Parsers/JSON.c` (3 sites):**
- JSkipObject key scratch — **kept** DefaultAllocator + comment (JSON keys unbounded by spec).
- JReadNumber digit accumulator — **kept** + comment (JSON numbers unbounded).
- JSkipValue parse-and-discard scratch — **kept** + comment (JSON strings unbounded).

**`Source/Misra/Sys/Dns.c` (2 sites):**
- `try_one_query` scratch — **switched to `ArenaAllocator`** (linear write-then-drop fits a bump arena).
- `DnsResolve_5` hostname-normalize — **switched to `StrInitStack` (256-byte stack)** with a pre-check `ZstrLen(hostname) >= 256` rejecting oversize input (LOG_ERROR + return) so the stack cap is provably unreachable. `HeapAllocator spill` wired per `StrInitStack` contract but dead by construction.

**`Source/Misra/Std/Io.c` (~12 sites):**
- High-level fmt drivers (`str_patch_fmt`, `buf_patch_fmt`, `f_write_fmt`, `f_read_fmt`) — **kept** + per-site comments (render-size is caller-controlled; `f_read_fmt` may load whole file).
- `_read_*` visitor functions — **kept** + block-comment above the section (the `TypeSpecificReader` function-pointer signature has no Allocator slot — public visitor protocol). DebugAllocator coverage preserved in debug builds.

### Task D — `append_hex16` deletion

**Outcome (b)** — added the missing fmt capability:
- Added `FMT_FLAG_ZERO_PAD = 1 << 10` in `Include/Misra/Std/Io.h`.
- Parser now consumes a leading `0` after `{:` as the zero-pad flag when followed by a width digit (`{016x}` → flag + width=16; `{0}` still parses as width-0).
- Integer formatters `_write_u64` / `_write_i64` honour the flag: suppress base prefix, pad with `'0'` via new `pad_numeric_zeros` helper that inserts fill **after** any leading sign — so `{016x}` of `0xabc` → `"0000000000000abc"`, `{08}` of `-42` → `"-0000042"`.
- `append_hex16` and its scaffolding deleted from `File.c`. `file_open_temp` is now a single `StrAppendFmt(out_path, "{}{016x}", prefix, Prng64())`.

**Files touched:** Include/Misra/Std/Io.h, Source/Misra/Std/Io.c, Source/Misra/Std/File.c, Source/Misra/Sys/Dns.c, Source/Misra/Parsers/JSON.c.

## Pending

- **Review**: subagent to verify EVERY item in the user's accumulated feedback list against the current source — `[VERIFIED]` / `[PARTIAL]` / `[MISSING]` with file:line evidence. Don't trust git-log claims; only source evidence counts.
